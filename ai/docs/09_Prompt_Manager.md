# 09. Prompt Manager

Centralizes instruction systems to avoid raw text prompts mixed into codebase logic files.

## Guidelines

- All prompts are defined as flat files or structured dictionary configs.
- Components lookup prompt templates dynamically.
- Parameter insertion is handled via clean string rendering patterns.
